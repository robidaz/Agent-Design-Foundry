# Anthropic Claude API (drafting + coverage/agreement judgment)
ANTHROPIC_API_KEY=sk-ant-...

# Local pgvector via docker-compose
PG_DSN=postgresql://knowledgex:knowledgex@localhost:5432/knowledgex

# Embeddings run locally via sentence-transformers — no API key required.
# Default model: BAAI/bge-large-en-v1.5 (set in assets/config.yaml).
